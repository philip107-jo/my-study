 ### 1.추상화 (Abstraction) : 본질만 남기기
 - **핵심:** 복잡한 현실 세계의 사물에서 필요한 공통점(속성·기능)만 추출하고, 불필요한 디테일은 과감히 쳐내는 설계 과정입니다.
 - **비유:** 자동차 엔진 내부 구조는 몰라도 '핸들'과 '페달'이라는 인터페이스만 알면 운전할 수 있는 것과 같습니다.
 - **구현:** 주로 인터페이스(Interface)나 추상 클래스(Abstract Class)로 큰 틀을 잡을 때 씁니다.
​

### 2.캡슐화 (Encapsulation) : 묶고 숨기기
- **핵심:** 데이터(변수)와 기능(메서드)을 하나의 캡슐처럼 묶고, 외부에서 함부로 조작하지 못하도록 은닉하는 것입니다.
- **비유:** 감기약 캡슐 안의 가루 성분 배합을 일반 사용자가 알 필요도 없고, 직접 만지면 변질될 위험이 있으니 껍질로 감싸 보호하는 것과 같습니다.
- **구현:** private 같은 접근 제어자로 데이터를 숨기고, 오직 getter / setter 메서드를 통해서만 안전하게 접근하도록 제한합니다.  
​
### 3.상속 (Inheritance) : 공통 기능 물려받기
- **핵심:** 상위(부모) 클래스가 가진 필드와 메서드를 하위(자식) 클래스가 그대로 물려받아 코드를 재사용하는 것입니다.
- **비유:** '스마트폰'이라는 부모 밑에 '아이폰', '갤럭시'라는 자식이 들어오는 구조입니다. 전화·문자 같은 기본 기능은 부모 걸 그대로 쓰고, 각자 필요한 고유 기능(애플페이, 삼성페이)만 새로 추가하면 됩니다.
- **효과:** 중복 코드가 획기적으로 줄어들고 유지보수가 편해집니다.
 ### 4.다형성 (Polymorphism) : 같은 모양, 다른 행동

- **핵심:** 하나의 객체나 메서드가 상황에 따라 여러 가지 형태로 동작할 수 있는 성질입니다. OOP의 유연성을 담당하는 가장 중요한 특징입니다.
- **비유:** 운전자가 '오른쪽 페달'을 밟는 행위는 같지만, 타고 있는 차가 '모닝'이냐 '포르쉐'냐에 따라 치고 나가는 반응(동작)이 완전히 다른 것과 같습니다.
- **구현:** 부모 메서드를 자식 입맛에 맞게 바꾸는 오버라이딩(Overriding), 같은 이름의 메서드를 매개변수만 다르게 해 여러 개 만드는 **오버로딩(Overloading)**이 있습니다. 





